import React from 'react';
import OpenCourseBlog from '../OpenCourseBlog';

const page = () => {
    return (
        <div>
            <OpenCourseBlog/>
        </div>
    );
};

export default page;